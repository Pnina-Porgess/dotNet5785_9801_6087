<Window x:Class="PL.Call.CallListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL.Call"
        mc:Ignorable="d"
        Title="CallListWindow" Height="640" Width="800"
        Loaded="Window_Loaded" Closed="Window_Closed"
        DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Filter Row -->
        <Grid Grid.Row="0" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="150"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="150"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="150"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="🧩 Field:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <ComboBox Grid.Column="1"
                      ItemsSource="{Binding Source={StaticResource CallFieldCollectionKey}}"
                      SelectedItem="{Binding SelectedField, Mode=TwoWay}"
                      VerticalAlignment="Center"/>



            <TextBlock Text="🛑 Status:" Grid.Column="4" Margin="15,0,5,0" VerticalAlignment="Center"/>
            <ComboBox Grid.Column="5"
                      ItemsSource="{Binding Source={StaticResource CallStatusCollection}}"
                      SelectedItem="{Binding SelectedStatus, Mode=TwoWay}"
                      VerticalAlignment="Center"/>
        </Grid>

        <!-- ListView -->
        <ListView Grid.Row="1" Margin="10"
                  ItemsSource="{Binding CallList}"
                  SelectedItem="{Binding SelectedCall}"
                  MouseDoubleClick="ListView_MouseDoubleClick">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="CallId" DisplayMemberBinding="{Binding CallId}" Width="80"/>
                    <GridViewColumn Header="CallType" DisplayMemberBinding="{Binding CallType}" Width="80"/>
                    <GridViewColumn Header="RemainingTime" DisplayMemberBinding="{Binding RemainingTime}" Width="80"/>
                    <GridViewColumn Header="LastVolunteerName" DisplayMemberBinding="{Binding LastVolunteerName}" Width="120"/>
                    <GridViewColumn Header="CompletionTime" DisplayMemberBinding="{Binding CompletionTime}" Width="100"/>
                    <GridViewColumn Header="CallStatus" DisplayMemberBinding="{Binding CallStatus}" Width="80"/>
                    <GridViewColumn Header="TotalAssignments" DisplayMemberBinding="{Binding TotalAssignments}" Width="100"/>
                    <GridViewColumn Header="🗑️" Width="60">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="🗑️" Click="DeleteButton_Click"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="בטל" Width="80">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="בטל" Click="UnassignButton_Click"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>

        <!-- Add Button -->
        <Button Content="הוסף קריאה" Grid.Row="2" Height="40" Width="120" Margin="10" Click="AddButton_Click"/>
    </Grid>
</Window>
