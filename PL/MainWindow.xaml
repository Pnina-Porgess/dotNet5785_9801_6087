<Window x:Class="PL.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800"
        DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}"
        Background="#FFF6F5F0">
    <Window.Resources>
        <!-- Japanese-inspired color palette: Sakura Pink and Aizome Blue -->
        <SolidColorBrush x:Key="SakuraPink" Color="#FFFADADD"/>
        <SolidColorBrush x:Key="AizomeBlue" Color="#FF223A5E"/>
        <Style TargetType="Button">
            <Setter Property="Background" Value="{StaticResource SakuraPink}"/>
            <Setter Property="Foreground" Value="{StaticResource AizomeBlue}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="8,0,8,0"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="BorderBrush" Value="{StaticResource AizomeBlue}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="16"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#FFF7F6F2"/>
            <Setter Property="Foreground" Value="{StaticResource AizomeBlue}"/>
            <Setter Property="FontSize" Value="16"/>
        </Style>
        <Style TargetType="Label">
            <Setter Property="Foreground" Value="{StaticResource AizomeBlue}"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="16"/>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="6*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>

        <!-- Left: Max RiskRange/Update -->
        <Border Grid.Column="0" Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Left" Background="{StaticResource SakuraPink}" Width="166" Height="160" CornerRadius="10" Margin="10,0,0,0">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <Label Content="Max RiskRange:" Margin="0,16,0,0"/>
                <TextBox Text="{Binding RiskRange}" Width="120" Margin="0,8,0,0"/>
                <Button Content="Update" Click="UpdateButton_Click" Margin="0,16,0,0"/>
            </StackPanel>
        </Border>

        <!-- Center: Clock and Add buttons in a row -->
        <Border Grid.Column="1" Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center" Background="#FFF6F5F0" Width="439" Height="306" CornerRadius="16" BorderBrush="{StaticResource AizomeBlue}" BorderThickness="1">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <Label Content="{Binding CurrentTime}" FontSize="30" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,24,0,24" Background="{StaticResource SakuraPink}" Padding="18,8" BorderBrush="{StaticResource AizomeBlue}" BorderThickness="1" />
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" >
                    <Button Content="➕ min" Click="btnAddOneMinute_Click"/>
                    <Button Content="➕ hr" Click="btnAddOneHour_Click"/>
                    <Button Content="➕ day" Click="btnAddOneDay_Click"/>
                    <Button Content="➕ mo" Click="btnAddOneMonth_Click"/>
                    <Button Content="➕ yr" Click="btnAddOneYear_Click"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Right: Reset/Init -->
        <Border Grid.Column="2" Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Left" Background="{StaticResource SakuraPink}" Margin="83,0,0,0" Width="136" Height="160" CornerRadius="10">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <Button Content="Init DB" Click="InitializeDatabase_Click" Margin="0,24,0,0"/>
                <Button Content="Reset DB" Click="ResetDatabase_Click" Margin="0,16,0,0"/>
            </StackPanel>
        </Border>

        <!-- Bottom center: Handle Calls/Volunteers -->
        <StackPanel Grid.Column="1" Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,12,0,0">
            <Button Content="Handle Calls" Click="OnHandleCallsButton_Click" Background="{StaticResource SakuraPink}" Foreground="{StaticResource AizomeBlue}" Width="120" Margin="8,0"/>
            <Button Content="Handle Volunteer" Click="OnHandleVolunteersButton_Click" Background="{StaticResource SakuraPink}" Foreground="{StaticResource AizomeBlue}" Width="140" Margin="8,0"/>
        </StackPanel>
    </Grid>
</Window>
