<Window x:Class="PL.Login.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="250" Width="350"
        DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Label Grid.Row="0" Grid.Column="0" Content="User ID:"/>
        <TextBox Grid.Row="0" Grid.Column="1" Margin="5"
                 Text="{Binding UserId, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

        <Label Grid.Row="1" Grid.Column="0" Content="Password:"/>
        <PasswordBox Grid.Row="1" Grid.Column="1" Margin="5"
                     PasswordChanged="PasswordBox_PasswordChanged"/>

        <Button Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Margin="0,15,0,0"
                Content="Login" Click="LoginButton_Click"/>
        <TextBlock Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2" Foreground="Red"
                   Text="{Binding ErrorMessage}" Margin="0,10,0,0"/>
    </Grid>
</Window>

