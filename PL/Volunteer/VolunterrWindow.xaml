<Window x:Class="PL.Volunteer.VolunteerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:PL.Volunteer"
        xmlns:bo="clr-namespace:BO;assembly=BL"
        Title="Volunteer" Height="450" Width="400"
        DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}">
    <Window.Resources>
        <!-- מקור לאנום Role -->
        <ObjectDataProvider x:Key="RoleValues" MethodName="GetValues" ObjectType="{x:Type sys:Enum}" xmlns:sys="clr-namespace:System;assembly=mscorlib">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="bo:Role"/>
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>
        <!-- מקור לאנום DistanceType -->
        <ObjectDataProvider x:Key="DistanceTypeValues" MethodName="GetValues" ObjectType="{x:Type sys:Enum}" xmlns:sys="clr-namespace:System;assembly=mscorlib">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="bo:DistanceType"/>
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>
    </Window.Resources>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Id -->
        <Label Grid.Row="0" Grid.Column="0" Content="Volunteer ID:"/>
        <TextBox Grid.Row="0" Grid.Column="1"
                 Text="{Binding CurrentVolunteer.Id,Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                  IsReadOnly="True"/>

        <!-- FullName -->
        <Label Grid.Row="1" Grid.Column="0" Content="Full Name:"/>
        <TextBox Grid.Row="1" Grid.Column="1"
                 Text="{Binding CurrentVolunteer.FullName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

        <!-- Phone -->
        <Label Grid.Row="2" Grid.Column="0" Content="Phone:"/>
        <TextBox Grid.Row="2" Grid.Column="1"
                 Text="{Binding CurrentVolunteer.Phone, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

        <!-- Email -->
        <Label Grid.Row="3" Grid.Column="0" Content="Email:"/>
        <TextBox Grid.Row="3" Grid.Column="1"
                 Text="{Binding CurrentVolunteer.Email, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

        <!-- Password -->
        <Label Grid.Row="4" Grid.Column="0" Content="Password:"/>
        <TextBox Grid.Row="4" Grid.Column="1"
                 Text="{Binding CurrentVolunteer.Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

        <!-- Address -->
        <Label Grid.Row="5" Grid.Column="0" Content="Address:"/>
        <TextBox Grid.Row="5" Grid.Column="1"
                 Text="{Binding CurrentVolunteer.CurrentAddress, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

        <!-- IsActive -->
        <Label Grid.Row="6" Grid.Column="0" Content="Is Active:"/>
        <CheckBox Grid.Row="6" Grid.Column="1"
                  IsChecked="{Binding CurrentVolunteer.IsActive, Mode=TwoWay}"/>

        <!-- Role -->
        <Label Grid.Row="7" Grid.Column="0" Content="Role:"/>
        <ComboBox Grid.Row="7" Grid.Column="1"
                  ItemsSource="{Binding Source={StaticResource RoleValues}}"
                  SelectedItem="{Binding CurrentVolunteer.Role, Mode=TwoWay}"/>

        <!-- DistanceType -->
        <Label Grid.Row="8" Grid.Column="0" Content="Distance Type:"/>
        <ComboBox Grid.Row="8" Grid.Column="1"
                  ItemsSource="{Binding Source={StaticResource DistanceTypeValues}}"
                  SelectedItem="{Binding CurrentVolunteer.DistanceType, Mode=TwoWay}"/>

        <!-- MaxDistance -->
        <Label Grid.Row="9" Grid.Column="0" Content="Max Distance:"/>
        <TextBox Grid.Row="9" Grid.Column="1"
                 Text="{Binding CurrentVolunteer.MaxDistance, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

        <Label Grid.Row="10" Grid.Column="0" Content="Call in progress"/>
        <Label Grid.Row="11" Grid.Column="0" Content="Assigment Id:"/>
        <TextBox Grid.Row="11" Grid.Column="1" Text="{Binding CurrentVolunteer.CurrentCall.Id, Mode=TwoWay}"  IsReadOnly="True"/>
        <Label Grid.Row="12" Grid.Column="0" Content="Call Id:"/>
        <TextBox Grid.Row="12" Grid.Column="1" Text="{Binding CurrentVolunteer.CurrentCall.CallId, Mode=TwoWay}"  IsReadOnly="True"/>
        <Label Grid.Row="13" Grid.Column="0" Content="Address"/>
        <TextBox Grid.Row="13" Grid.Column="1" Text="{Binding CurrentVolunteer.CurrentCall.Address, Mode=TwoWay}" IsReadOnly="True"/>



        <!-- Add/Update Button -->
        <Button Grid.Row="9" Grid.Column="0" Grid.ColumnSpan="2" Margin="0,90,0,0"
                Content="{Binding ButtonText}" Click="btnAddUpdate_Click"/>
    </Grid>
</Window>